# Contributing

Create a focused branch, install the project with `pip install -e .` and
`pip install -r requirements.txt`, then run `make lint`, `make test`, and `make verify`
before opening a pull request. Keep proprietary plant inputs under the gitignored data
directories, add tests for behavioral changes, and update the relevant methodology or
product documentation when a public interface or generated artifact changes.
